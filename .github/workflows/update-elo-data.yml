name: Update ELO Data

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  update-elo-data:
    runs-on: ubuntu-latest
    steps:
      - name: Merged into Update TA Light Data
        run: |
          echo "TA ELO update is now part of .github/workflows/update-ta-rankings.yml."
          echo "Run 'Update TA Light Data' to update TA rankings and TA ELO."
