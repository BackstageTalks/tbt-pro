name: Update TA Rankings

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  update-ta-rankings:
    runs-on: ubuntu-latest
    steps:
      - name: Merged into Update TA Data
        run: |
          echo "TA rankings update is now part of .github/workflows/update-ta-data.yml."
          echo "Run 'Update TA Data' to update rankings, TA profiles and TA ELO in one batch."
